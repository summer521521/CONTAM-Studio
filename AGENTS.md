# CONTAM Studio项目规则

本文件只补充项目特有规则；全局AGENTS规则继续适用。

## 目标与范围

- 项目服务于使用CONTAM开展教学与科研的学生、教师、研究人员和研究生。
- 当前产品是Windows优先、本地优先、联网增强、中英文双语的CONTAM桌面工作台，不是通用BIM或多求解器平台。
- 按可独立验证的纵向切片推进任务，未经当前阶段批准不得提前搭建未来功能。

## 不可突破的边界

- 必须使用官方ContamX承担数值求解，禁止重写CONTAM求解器。
- 前端和AI禁止直接修改原始PRJ文本；默认在草稿、内存模型或副本中编辑。
- GUI与AI必须共用同一套语义化领域接口，不得建立绕过领域验证的写入路径。
- 未知或暂不支持的PRJ内容不得静默丢失；无法可靠保存时必须保持只读。
- 默认离线、最小权限；联网AI只能由用户主动配置和启用。
- AI写入必须经过结构化Patch、Diff、确定性验证和用户确认，并保留快照与追踪信息。

## 工程规则

- 新增依赖前必须评估必要性、许可证、维护状态和桌面打包成本。
- 测试不得修改用户唯一版本的真实工程文件，应使用副本、临时工作区或明确的测试夹具。
- 不得声称运行了未实际运行的测试；区分“检查通过”“关键问题修复”和“完整审计完成”。
- 与项目无关的临时文件、一次性脚本、下载、截图、缓存和调试输出只能放入`F:\Codex_File`下的分类目录。
- 后续正式Codex开发任务必须维护`docs/development/task-log/`任务日志：开始时记录`in_progress`，结束时记录状态、UTC时间、耗时、摘要和验证；Token仅记录客户端明确提供的精确数据，不读取敏感或隐藏数据；日志更新应与本任务代码修改一起提交。
- Codex负责代码、自动测试、构建和非GUI命令行验证；桌面GUI验收默认由用户按明确清单手动完成并记录为`pending_user`，不得声称Codex已完成真实手动验收。
- 除非用户明确要求，GUI验收不得使用Computer Use、坐标点击、图像识别或反复截图，也不为纯GUI验证新增或更新截图；用户反馈可记录在PR评论和最终验收结论中，无需为纯状态变化单独提交。
