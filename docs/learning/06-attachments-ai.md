# 06 附件与 AI

附件先进入 owned quarantine，再做类型、魔数、大小、压缩比和主动内容检查。PDF、Office、ZIP、表格和图片都是不可信数据，不执行宏、JavaScript、公式、外链或嵌入二进制。

AI 只看到用户选中的 EvidenceBundle：有哈希、引用、期限和上限。DomainToolGateway 只有固定语义只读工具；Patch 或 ActionBundle 必须经过精确哈希、用户和一次性批准。

问题：当 AI 要求“读取整个硬盘”时，哪条边界先拒绝它？
