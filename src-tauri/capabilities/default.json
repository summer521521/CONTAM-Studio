{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the local workbench and user-initiated PRJ selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
